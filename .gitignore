node_modules/
dist/
.DS_Store
.env
.env.*
*.log
coverage/
.cache/
.tmp/
input/**/raw-private/
input/**/pii/

# Regenerated export outputs (npm run export:*) — never commit, only the
# committed exports/**/samples/*.sample.json reference files are tracked.
exports/**/output/

# superpowers brainstorming companion
.superpowers/
/.playwright-mcp

# Claude Code agent worktrees — local, ephemeral, per-session; never commit
.claude/worktrees/

# Next.js build cache (itinerary-builder) — regenerable, never commit
itinerary-builder/.next/
itinerary-builder/node_modules/
itinerary-builder/tsconfig.tsbuildinfo
.next/
*.tsbuildinfo
